# Note

Host the website using live-server with auto reload:

```bash
npx live-server .
```

# 维护备忘

- **论文**：在 `index.html` 的 `#publications` 区块按 `pub-entry` 结构增删，注意同步更新末尾的统计行（`7 papers · 3 first author · 1 patent`）
- **开源项目**：`#opensource` 区块，star 数写静态值即可，线上会被 `js/stars.js` 动态覆盖；新增仓库时给 `.os-star` 加 `data-repo="仓库名"`
- **简历**：PDF 在 `assets/` 下，文件名带日期；如需在页面提供下载，记得同步更新链接
- **主题色**：统一在 `css/variables.css` 修改，不要在别处硬编码颜色
